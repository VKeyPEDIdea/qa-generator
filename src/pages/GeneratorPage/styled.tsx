import React from 'react';
import classes from './GeneratorPage.module.css';
import createStyledComponent from 'shared/utils/createStyledComponent';

export const Container = createStyledComponent(classes['container']);
export const TopNavigation = createStyledComponent(classes['top-nav']);
export const ActionBar = createStyledComponent(classes['action-bar']);
export const ActionBarItem = createStyledComponent(classes['action-bar__item']);
